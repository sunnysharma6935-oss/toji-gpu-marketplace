version: "3.8"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: gpumarket
      POSTGRES_PASSWORD: gpumarket
      POSTGRES_DB: gpumarket
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
